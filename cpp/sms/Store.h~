#include "itemEntry.h"
#include "Main.h"

class Store{
public:
  int numItem;
  itemEntry database[MAX];

  Store(){
    numItem = 0;
  }
  void insertItem( string itemName, string company, int c,float p);
  void deleteItem( string itemName);
  itemEntry *search( string itemName);
  void updateItem(string itemName, int total, float price);
};
